<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta
      name="description"
      content="Free online tool to compress your images while maintaining quality. Reduce file size for faster websites and easier sharing."
    />
    <meta
      name="keywords"
      content="image compressor, image optimization, reduce image size, compress images, image tools"
    />
    <title>Image Compressor - Optimize Your Images Online | Free Tool</title>
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"
    />

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        color: #333;
        line-height: 1.6;
        min-height: 100vh;
        padding: 20px;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
      }

      header {
        text-align: center;
        margin-bottom: 40px;
        padding: 20px;
      }

      header h1 {
        font-size: 2.8rem;
        color: #2c3e50;
        margin-bottom: 10px;
      }

      header p {
        font-size: 1.2rem;
        color: #34495e;
        max-width: 800px;
        margin: 0 auto;
      }

      .compressor-container {
        display: flex;
        flex-wrap: wrap;
        gap: 30px;
        justify-content: center;
        background: white;
        border-radius: 15px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        padding: 30px;
        margin-bottom: 40px;
      }

      .upload-section {
        flex: 1;
        min-width: 300px;
        padding: 20px;
        text-align: center;
        border-right: 1px solid #eee;
      }

      .preview-section {
        flex: 1;
        min-width: 300px;
        padding: 20px;
        text-align: center;
      }

      .drop-area {
        border: 3px dashed #3498db;
        border-radius: 12px;
        padding: 40px 20px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s;
        background-color: #f8f9fa;
      }

      .drop-area:hover {
        background-color: #e8f4fc;
        border-color: #2980b9;
      }

      .drop-area i {
        font-size: 60px;
        color: #3498db;
        margin-bottom: 15px;
      }

      .drop-area h3 {
        margin-bottom: 15px;
        color: #2c3e50;
      }

      .drop-area p {
        color: #7f8c8d;
        margin-bottom: 20px;
      }

      .file-input {
        display: none;
      }

      .select-button {
        background: #3498db;
        color: white;
        padding: 12px 25px;
        border-radius: 50px;
        cursor: pointer;
        font-weight: 600;
        display: inline-block;
        transition: all 0.3s;
        border: none;
      }

      .select-button:hover {
        background: #2980b9;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .slider-container {
        margin: 30px 0;
        padding: 0 20px;
      }

      .slider-container label {
        display: block;
        margin-bottom: 15px;
        font-weight: 600;
        color: #2c3e50;
      }

      .slider-value {
        font-weight: bold;
        color: #3498db;
        font-size: 1.2rem;
      }

      .quality-slider {
        width: 100%;
        height: 10px;
        -webkit-appearance: none;
        appearance: none;
        background: #dfe6e9;
        outline: none;
        border-radius: 5px;
      }

      .quality-slider::-webkit-slider-thumb {
        -webkit-appearance: none;
        appearance: none;
        width: 22px;
        height: 22px;
        border-radius: 50%;
        background: #3498db;
        cursor: pointer;
        transition: all 0.2s;
      }

      .quality-slider::-webkit-slider-thumb:hover {
        background: #2980b9;
        transform: scale(1.1);
      }

      .compress-button {
        background: #2ecc71;
        color: white;
        padding: 15px 35px;
        border-radius: 50px;
        cursor: pointer;
        font-weight: 600;
        font-size: 1.1rem;
        border: none;
        transition: all 0.3s;
        margin: 20px 0;
        width: 100%;
      }

      .compress-button:hover {
        background: #27ae60;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .compress-button:disabled {
        background: #95a5a6;
        cursor: not-allowed;
      }

      .preview-image {
        max-width: 100%;
        max-height: 300px;
        border-radius: 8px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        margin: 20px 0;
      }

      .image-info {
        background: #f8f9fa;
        border-radius: 8px;
        padding: 15px;
        margin: 20px 0;
        text-align: left;
      }

      .image-info h4 {
        margin-bottom: 10px;
        color: #2c3e50;
        border-bottom: 1px solid #ddd;
        padding-bottom: 8px;
      }

      .image-info p {
        margin: 8px 0;
        color: #34495e;
      }

      .download-button {
        background: #9b59b6;
        color: white;
        padding: 15px 35px;
        border-radius: 50px;
        cursor: pointer;
        font-weight: 600;
        font-size: 1.1rem;
        border: none;
        transition: all 0.3s;
        margin: 20px 0;
        width: 100%;
        display: none;
      }

      .download-button:hover {
        background: #8e44ad;
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
      }

      .features {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-bottom: 40px;
        justify-content: center;
      }

      .feature-box {
        flex: 1;
        min-width: 250px;
        background: white;
        padding: 25px;
        border-radius: 12px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.05);
        text-align: center;
      }

      .feature-box i {
        font-size: 40px;
        color: #3498db;
        margin-bottom: 15px;
      }

      .feature-box h3 {
        margin-bottom: 15px;
        color: #2c3e50;
      }

      footer {
        text-align: center;
        padding: 20px;
        color: #7f8c8d;
        border-top: 1px solid #ddd;
        margin-top: 40px;
      }

      @media (max-width: 768px) {
        .upload-section {
          border-right: none;
          border-bottom: 1px solid #eee;
          padding-bottom: 30px;
        }
        header h1 {
          font-size: 2.2rem;
        }
        header p {
          font-size: 1rem;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Image Compressor</h1>
        <p>
          Free online tool to compress your images while maintaining quality.
          Reduce file size for faster websites and easier sharing.
        </p>
      </header>

      <div class="compressor-container">
        <div class="upload-section">
          <div
            class="drop-area"
            onclick="document.getElementById('file-input').click()"
          >
            <i class="fas fa-cloud-upload-alt"></i>
            <h3>Drag & Drop your images here</h3>
            <p>or click below to upload</p>
            <input
              type="file"
              class="file-input"
              id="file-input"
              accept="image/*"
            />
            <button class="select-button">Select Image</button>
          </div>
        </div>

        <div class="preview-section">
          <img id="preview-image" class="preview-image" src="" alt="" />
          <div class="image-info" id="image-info" style="display: none">
            <h4>Image Info</h4>
            <p id="original-size">Original Size: -</p>
            <p id="compressed-size">Compressed Size: -</p>
          </div>
          <div class="slider-container">
            <label for="quality"
              >Compression Quality:
              <span id="slider-value" class="slider-value">80</span>%</label
            >
            <input
              type="range"
              id="quality"
              class="quality-slider"
              min="10"
              max="100"
              value="80"
            />
          </div>
          <button id="compress-button" class="compress-button" disabled>
            Compress Image
          </button>
          <button id="download-button" class="download-button">
            Download Image
          </button>
        </div>
      </div>

      <div class="features">
        <div class="feature-box">
          <i class="fas fa-bolt"></i>
          <h3>Fast Compression</h3>
          <p>Reduce image size instantly with smart compression.</p>
        </div>
        <div class="feature-box">
          <i class="fas fa-lock"></i>
          <h3>Secure</h3>
          <p>Images never leave your browser. 100% private and safe.</p>
        </div>
        <div class="feature-box">
          <i class="fas fa-mobile-alt"></i>
          <h3>Responsive</h3>
          <p>Works perfectly on mobile, tablet, and desktop devices.</p>
        </div>
      </div>

      <footer>© 2025 Image Compressor | Made by I</footer>
    </div>

    <script>
      const fileInput = document.getElementById("file-input");
      const previewImage = document.getElementById("preview-image");
      const imageInfo = document.getElementById("image-info");
      const originalSize = document.getElementById("original-size");
      const compressedSize = document.getElementById("compressed-size");
      const compressButton = document.getElementById("compress-button");
      const downloadButton = document.getElementById("download-button");
      const slider = document.getElementById("quality");
      const sliderValue = document.getElementById("slider-value");

      let uploadedImage = null;
      let compressedBlob = null;

      slider.addEventListener("input", () => {
        sliderValue.textContent = slider.value;
      });

      fileInput.addEventListener("change", (e) => {
        const file = e.target.files[0];
        if (!file) return;
        const reader = new FileReader();
        reader.onload = (event) => {
          previewImage.src = event.target.result;
          previewImage.style.display = "block";
          imageInfo.style.display = "block";
          originalSize.textContent = `Original Size: ${(
            file.size / 1024
          ).toFixed(2)} KB`;
          compressedSize.textContent = `Compressed Size: -`;
          compressButton.disabled = false;
          uploadedImage = file;
        };
        reader.readAsDataURL(file);
      });

      compressButton.addEventListener("click", () => {
        if (!uploadedImage) return;

        const quality = slider.value / 100;
        const canvas = document.createElement("canvas");
        const ctx = canvas.getContext("2d");
        const img = new Image();
        img.src = URL.createObjectURL(uploadedImage);

        img.onload = () => {
          canvas.width = img.width;
          canvas.height = img.height;
          ctx.drawImage(img, 0, 0);

          canvas.toBlob(
            (blob) => {
              compressedBlob = blob;
              previewImage.src = URL.createObjectURL(blob);
              compressedSize.textContent = `Compressed Size: ${(
                blob.size / 1024
              ).toFixed(2)} KB`;
              downloadButton.style.display = "block";
            },
            "image/jpeg",
            quality
          );
        };
      });

      downloadButton.addEventListener("click", () => {
        if (!compressedBlob) return;
        const a = document.createElement("a");
        a.href = URL.createObjectURL(compressedBlob);
        a.download = "compressed.jpg";
        a.click();
      });
    </script>
  </body>
</html>
